# ============================================
# ZAGFER - VARIÁVEIS DE AMBIENTE
# ============================================
#
# INSTRUÇÕES:
# 1. Copie este arquivo: cp .env.example .env
# 2. Preencha os valores conforme seu ambiente
# 3. NUNCA commite o arquivo .env no Git!
#
# ============================================

# --------------------------------------------
# AMBIENTE
# --------------------------------------------
# Valores: development | staging | production
NODE_ENV=development

# --------------------------------------------
# API
# --------------------------------------------
API_PORT=3000
API_HOST=0.0.0.0

# URL pública da API (usada pelo frontend)
API_URL=http://localhost:3000

# --------------------------------------------
# DATABASE - POSTGRESQL
# --------------------------------------------
# Nome do banco de dados
DB_NAME=zagfer

# Usuário do banco
DB_USER=zagfer

# Senha do banco (MUDE EM PRODUÇÃO!)
DB_PASSWORD=changeme

# Host (use 'zagfer-db' dentro do Docker)
DB_HOST=zagfer-db

# Porta (use 5432 dentro do Docker)
DB_PORT=5432

# Porta para acesso externo (apenas dev)
DB_PORT_HOST=5432

# URL completa de conexão
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# --------------------------------------------
# SEGURANÇA
# --------------------------------------------
# Secret para JWT (MÍNIMO 32 caracteres em produção!)
# Gere com: openssl rand -hex 32
JWT_SECRET=dev-secret-change-in-production-min-32-chars

# Tempo de expiração do JWT
JWT_EXPIRATION=7d

# Secret para sessões
# Gere com: openssl rand -base64 32
SESSION_SECRET=dev-session-change-in-production

# Origens permitidas pelo CORS (separadas por vírgula)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# --------------------------------------------
# FEATURES
# --------------------------------------------
# Habilitar telemetria (métricas, analytics)
ENABLE_TELEMETRY=false

# Habilitar logs de debug
ENABLE_DEBUG_LOGS=true

# Habilitar hot reload (apenas dev)
ENABLE_HOT_RELOAD=true

# --------------------------------------------
# LOGS
# --------------------------------------------
# Nível de log: error | warn | info | debug
LOG_LEVEL=info

# Formato de log: json | pretty
LOG_FORMAT=pretty

# --------------------------------------------
# DOCKER
# --------------------------------------------
# Prefixo do nome dos containers
COMPOSE_PROJECT_NAME=zagfer

# --------------------------------------------
# NGINX (SE USAR)
# --------------------------------------------
# NGINX_PORT=80
# NGINX_SSL_PORT=443
# SSL_CERT_PATH=/etc/nginx/certs/cert.pem
# SSL_KEY_PATH=/etc/nginx/certs/key.pem