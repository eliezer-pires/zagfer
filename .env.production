# URL da API de Produção (Onde seu servidor backend estará hospedado)
# IMPORTANTE: Não pode ser localhost! Deve ser algo como: https://api.zagfer.com
VITE_API_URL=https://DOMINIO_DO_BACKEND

# Configuração do Banco de Dados em PRODUÇÃO
# Usa arquivo prod.db ou outro caminho para produção
DATABASE_URL="file:./prisma/prod.db"

# Flag para indicar que deve usar dados iniciais (initialData.ts)
# Em produção, o backend carregará dados de data/initialData.ts
USE_INITIAL_DATA=true
